"""
This module defines the database models for the application.

It utilizes Django's ORM (Object-Relational Mapper) to create
database tables based on these model definitions. Each model
represents a table in the database, and fields on the model
represent columns in the table.
"""

# Create your models here.
